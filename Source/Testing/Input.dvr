Test number 000
InflowWind driver input file.  V1.00
   t           echo
------------------------------------------------------------------------------
"Input.ipt"    IfWFileName    -- IfW input filename
   20          NumTSteps      -- number of timesteps to run (DEFAULT for all)       (-)
   0.0         TStart         -- Start time                                         (s)
   T           Summary        -- Summarize the data extents in the windfile
   T           SummaryFile    -- Write summary to file (.dvr.sum)
----  IfW inputs -------------------------------------------------------------
   0.0025      DT             -- timestep size for driver to take (or DEFAULT for what the file contains)
   80          TurbineHeight  -- Height of the turbine.  Used for error checking.   (m)
   50          Width          -- Width of the windfield needed                      (m)
----  FFT at a point (uses DT from above, not the DT of the windfile) --------
   t           FFTcalc        -- calculate the FFT of the data
   0,10.0,70.0       FFTcoord       -- X,Y,Z coordinate to calculate the FFT at           (m)
----  Output grid (Points below ground will simply be ignored) ---------------
   t           WindGrid       -- report wind data at set of Y,Z coordinates
   0,0,100      GridCtrCoord   -- coordinate of center of grid                       (m)
   10,1        GridDY,GridDZ  -- Stepsize of grid                                   (m)
   11,11       GridNY,GridNZ  -- number of grid points in Y and Z directions        (-)
END of driver input file
