Test number 000
InflowWind driver input file.  V1.00
   t           echo
------------------------------------------------------------------------------
"Input.ipt"    IfWFileName    -- IfW input filename
   DEFAULT     NumTSteps      -- number of timesteps to run (DEFAULT for all)       (-)
   0.0         TStart         -- Start time                                         (s)
   f           Summary        -- Summarize the data extents in the windfile
   f           SummaryFile    -- Write summary to file (.dvr.sum)
----  IfW inputs -------------------------------------------------------------
   default 0.0025      DT             -- timestep size for driver to take (or DEFAULT for what the file contains)
----  FFT at a point (uses DT from above, not the DT of the windfile) --------
   t           FFTcalc        -- calculate the FFT of the data
   0,10.1,70.0 FFTcoord       -- X,Y,Z coordinate to calculate the FFT at           (m)
----  Output grid (Points below ground will simply be ignored) ---------------
   t           WindGrid       -- report wind data at set of Y,Z coordinates
   0,0,100     GridCtrCoord   -- coordinate of center of grid                       (m)
   0,10,1      GridDx,GridDY,GridDZ  -- Stepsize of grid                            (m)
   0,11,41     GridNx,GridNY,GridNZ  -- number of grid points in Y and Z directions (-)
END of driver input file
